#======================================
# configurare ambiente conda
#======================================

conda create --name rootjuice2 python=3.9
conda activate rootjuice2
conda install -c conda-forge scrapy

// per far funzionare rootjuice2
pip install tika-app
pip install BeautifulSoup4
pip install pysolr
pip install psycopg2-binary

// per far funzionare TDM_generator
conda install nltk

// per far funzionare SdgDetector2
conda install scikit-learn
conda install pandas